{
  "name": "MeroHostel",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "start": "concurrently -n \"api,web\" -c \"green,cyan\" \"yarn workspace api start\" \"yarn workspace web start\"",
    "dev": "concurrently -n \"api,web\" -c \"green,cyan\" \"yarn workspace api run dev\" \"yarn workspace web dev\""
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.2",
    "@commitlint/config-conventional": "^17.0.2",
    "concurrently": "^7.2.0"
  }
}

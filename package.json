{
  "name": "MeroHostel",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n \"api,web\" -c \"green,cyan\" \"npm run dev --workspace api\" \"npm run dev --workspace web\""
  },
  "devDependencies": {
    "concurrently": "^7.2.0"
  }
}
